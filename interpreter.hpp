#include <iostream>
#include <optional>
#include "node.hpp"

namespace snowlang::interpreter
{
    // void interpret(std::unique_ptr<Node> ast);

    // class SymbolTable;

    // struct Symbol
    // {
    //     std::string name;
    //     std::unique_ptr<SymbolTable> attr{nullptr};
    // };

    // class SymbolTable
    // {
    // public:
    //     void addSymbol(std::string name, )
    // private:
    //     std::vector<Symbol> symbols;
    // };
}